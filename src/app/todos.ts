export interface Todos {
    id: number;
    description: string;
    completed: boolean;
}

export const todos = [
    {
        id: 1,
        description: "Walk the dog",
        completed: false
    }
]